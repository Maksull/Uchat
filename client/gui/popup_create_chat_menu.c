#include "../inc/client.h"

// Function to popup create chat menu
void popup_create_chat_menu(GtkWidget *widget, gpointer chat_screen)
{
    if (widget) { }
    (void)chat_screen;

    build_create_chat_menu(); // build chat menu
}

#include "../inc/server.h"

// Function to handle new client creation
void create_new_client(SSL* ssl, int client_socket) {
    // Accept SSL connection
    //if () {
        // If SSL accept fails, log the error and exit the program
        // Log SSL accept error. handle_error function
        // Exit the program with failure status
    //}
    
    // Set client socket to non-blocking mode
    // Get the socket flags
    //if () { // Check if getting flags failed
        // If getting flags fails, log the error
        // Log error
    //}
    // Set socket to non-blocking mode
    // Set socket file descriptor to non-blocking mode

    // Allocate memory for server utils struct
    // Allocate memory for server utilities
    // Assign client socket to server utilities
    // Assign SSL connection to server utilities
    // Initialize user to NULL

    // Declare a thread variable to hold the thread ID
    // Create a new thread to handle the client
    // Create a new thread
}

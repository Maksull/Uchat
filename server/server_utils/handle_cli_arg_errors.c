#include "../inc/server.h"

// Function to handle command-line argument errors
void handle_cli_arg_errors(char** argv) {
    //if (argv[1] == NULL) { // Check if port number is not provided
        // If port number is not provided, print usage message and exit the program
    //mx_printerr("usage: ./uchat_server [port]\n"); // Print usage message to stderr
        // Exit the program with failure status
    //} 
}

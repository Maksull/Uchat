#include "./inc/server.h"

int main(int argc, char *argv[]) {
    // Check for command-line argument errors. handle_arg_errors function
    // Suppress unused parameter warning

    // Declare and initialize variables
    // Server and client address structures
    // Client socket descriptor
    // Size of address structure

    // Initialize daemon

    // Initialize SSL context and SSL structure
    // SSL context
    // SSL structure
    // Initialize SSL context

    // Set up server address structure
    // Set address family to IPv4
    // Bind to any available local IP address
    // Convert port number to network byte order

    // Initialize server socket

    // Accept incoming connections and handle them indefinitely
    //while (1) {
        // Accept incoming client connections
        //if () {
            // Initialize SSL connection
            // Create a new SSL structure
            // Associate SSL structure with the client socket

            // Create a new client thread. new_client_create function
        //} else {
            // Handle errors if accepting client connection fails
            // Log error. handle_error function
        //}
    //}

    // Return EXIT_FAILURE
}
